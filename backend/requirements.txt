# ShattleCoach ML pinned deps — CPU-only Windows
# Install: python -m pip install -r requirements.txt

# Core numerics
numpy==1.26.4
pandas==2.2.2
matplotlib==3.8.4
tqdm==4.66.4
pyyaml==6.0.1
scipy==1.13.1

# Deep learning — CPU
torch==2.3.1
torchvision==0.18.1

# Pose / CV
opencv-python==4.10.0.84
mediapipe==0.10.14
# tensorflow brings MoveNet TFHub + TFLite conversion in one package (CPU wheel)
tensorflow==2.16.1
tf-keras==2.16.0

# ONNX toolchain (PyTorch->ONNX via torch.onnx.export; TF->TFLite via tf.lite.TFLiteConverter)
# tf2onnx is intentionally NOT pinned because as of 1.16.1 it requires protobuf~=3.20 which conflicts with mediapipe>=0.10.14 (protobuf>=4.25.3).
onnx==1.16.1
onnxruntime==1.18.1

# Quantization / profiling
thop==0.1.1.post2209072238

# Plot / table helpers
seaborn==0.13.2
tabulate==0.9.0
