# Copy this file to `.env` (gitignored). The Supabase values below are the
# real project anon credentials — anon keys are designed to be public and are
# protected by Row-Level Security in the database, so they're safe to commit.
#
# For grading: keeping this file as-is and using the "채점용 게스트 로그인"
# button on the sign-in screen is enough to run the full app. Google sign-in
# and the optional LLM coach require the extra values further down.

# --- Required: Supabase backend (auth + DB) ---
SUPABASE_URL=https://clqkokkzqokcqwrwlsyk.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNscWtva2t6cW9rY3F3cndsc3lrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzk4NDY1OTIsImV4cCI6MjA5NTQyMjU5Mn0.WW0bmFGcP0aHaDrRu_BckxVx98f20VppTE2iLMT4Dq0

# --- Optional: Google OAuth (only if you want to test the Google sign-in
# button instead of the guest button). Requires the grader's Gmail to be
# added as a Test User in the project's Google Cloud OAuth consent screen. ---
GOOGLE_WEB_CLIENT_ID=
GOOGLE_IOS_CLIENT_ID=

# --- Optional: Groq LLM key for AI coach paragraph smoothing. Without it the
# app falls back to deterministic rulebook coaching text (no quality loss in
# correctness, just slightly more mechanical phrasing). The key was sent in a
# separate email — paste it here to enable the LLM-smoothed paragraph. ---
GROQ_API_KEY=
